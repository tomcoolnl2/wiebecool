#
query CollectionPageBySlug($slug: String!) {
	collectionPageCollection(where: { slug: $slug }, limit: 1) {
		items {
			slug
			name
			title
			subtitle
			description {
				json
			}
			tags
		}
	}
}
